<% if signed_in? && current_user.team %>
  <h1><%= current_user.team.name %></h1>
  <% current_user.team.players.each do |player| %>
		<tr>
			<th>
				<%= link_to player.name, player_path(player) -%>
			</th>

		</tr>
	<% end %>
<% else %>
  <%= link_to 'Create Team', new_team_path %>
<% end %>
